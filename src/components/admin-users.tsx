"use client";
import { FormEvent, useState } from "react";
import { RippleButton, Status } from "@/components/ui";
type User = { id: string; username: string; role: "user" | "admin"; enabled: boolean; createdAt: string | Date; lastLoginAt: string | Date | null; batchCount: number; totalBytes: number };
export function AdminUsers({ initialUsers }: { initialUsers: User[] }) { const [users, setUsers] = useState<User[]>(initialUsers); const [q, setQ] = useState(""); const [message, setMessage] = useState(""); const [loading, setLoading] = useState(false); const [form, setForm] = useState({ username: "", password: "", role: "user", enabled: true }); async function load(search = q) { setLoading(true); const response = await fetch(`/api/admin/users?q=${encodeURIComponent(search)}`); const data = await response.json(); setLoading(false); if (!response.ok) setMessage(data.error); else setUsers(data.users); } async function create(event: FormEvent) { event.preventDefault(); const response = await fetch("/api/admin/users", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) }); const data = await response.json(); if (!response.ok) { setMessage(data.error); return; } setForm({ username: "", password: "", role: "user", enabled: true }); setMessage(`Created ${data.user.username}.`); void load(); } async function patch(id: string, body: object) { const response = await fetch(`/api/admin/users/${id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); const data = await response.json(); if (!response.ok) setMessage(data.error); else { setMessage(`Updated ${data.user.username}.`); void load(); } } return <div className="admin-users"><header className="page-header"><div><span className="eyebrow">ACCESS CONTROL</span><h1>User management</h1><p>Create private accounts, control access, and revoke active sessions when credentials change.</p></div></header><section className="user-admin-grid"><form className="create-user material-surface" onSubmit={create}><span className="eyebrow">CREATE ACCOUNT</span><h2>Provision access</h2><label>Username<input value={form.username} onChange={(e) => setForm({ ...form, username: e.target.value.toLowerCase() })} pattern="[a-z0-9._-]{2,64}" required /></label><label>Initial password<input type="password" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} minLength={10} autoComplete="new-password" required /></label><label>Role<select value={form.role} onChange={(e) => setForm({ ...form, role: e.target.value as "user" | "admin" })}><option value="user">Client user</option><option value="admin">Administrator</option></select></label><label className="check"><input type="checkbox" checked={form.enabled} onChange={(e) => setForm({ ...form, enabled: e.target.checked })} /><span />Enable this account now</label><RippleButton className="button-primary" type="submit">Create user</RippleButton></form><section className="material-surface users-table"><div className="panel-head"><div><span className="eyebrow">DIRECTORY</span><h2>Accounts</h2></div><form className="search" onSubmit={(e) => { e.preventDefault(); void load(); }}><input value={q} onChange={(e) => setQ(e.target.value)} placeholder="Search username" /><button>Search</button></form></div>{message && <p className="queue-message" role="status">{message}</p>}<div className="table-wrap"><table><thead><tr><th>User</th><th>Role</th><th>Status</th><th>Uploads</th><th>Last login</th><th>Action</th></tr></thead><tbody>{loading ? <tr><td colSpan={6}>Loading accounts…</td></tr> : users.map((user) => <tr key={user.id}><td><b>{user.username}</b><small>Created {new Intl.DateTimeFormat(undefined, { dateStyle: "medium" }).format(new Date(user.createdAt))}</small></td><td>{user.role}</td><td><Status tone={user.enabled ? "green" : "red"}>{user.enabled ? "enabled" : "disabled"}</Status></td><td>{user.batchCount}</td><td>{user.lastLoginAt ? new Intl.DateTimeFormat(undefined, { dateStyle: "medium" }).format(new Date(user.lastLoginAt)) : "Never"}</td><td><button className="table-action" onClick={() => patch(user.id, { enabled: !user.enabled })}>{user.enabled ? "Disable" : "Enable"}</button><button className="table-action" onClick={() => { const password = window.prompt(`New password for ${user.username} (10+ characters)`); if (password) void patch(user.id, { password }); }}>Reset</button></td></tr>)}</tbody></table></div></section></section></div>; }
