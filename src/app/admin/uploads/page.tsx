import { desc, eq } from "drizzle-orm";
import { redirect } from "next/navigation";
import { currentUser } from "@/lib/auth";
import { db } from "@/lib/db";
import { uploadBatches, users } from "@/lib/db/schema";
import { AppShell } from "@/components/app-shell";
import { Status } from "@/components/ui";
export default async function AdminUploads() { const user = await currentUser(); if (!user) redirect("/login"); if (user.role !== "admin") redirect("/upload"); const batches = await db().select({ id: uploadBatches.id, displayName: uploadBatches.displayName, status: uploadBatches.status, fileCount: uploadBatches.fileCount, totalBytes: uploadBatches.totalBytes, notificationStatus: uploadBatches.notificationStatus, createdAt: uploadBatches.createdAt, driveFolderId: uploadBatches.driveFolderId, username: users.username }).from(uploadBatches).innerJoin(users, eq(users.id, uploadBatches.userId)).orderBy(desc(uploadBatches.createdAt)); return <AppShell user={user}><header className="page-header"><div><span className="eyebrow">DELIVERY LEDGER</span><h1>Upload batches</h1><p>Administrative metadata and secure Drive handoff. Never render untrusted client files here.</p></div></header><section className="admin-panel material-surface"><div className="table-wrap"><table><thead><tr><th>Client</th><th>Batch</th><th>State</th><th>Size</th><th>Notice</th><th>Drive</th></tr></thead><tbody>{batches.map((batch) => <tr key={batch.id}><td>{batch.username}</td><td><b>{batch.displayName}</b><small>{batch.fileCount} files · {new Intl.DateTimeFormat(undefined, { dateStyle: "medium" }).format(batch.createdAt)}</small></td><td><Status tone={batch.status === "completed" ? "green" : batch.status.includes("fail") ? "red" : "orange"}>{batch.status.replace("_", " ")}</Status></td><td>{(batch.totalBytes / 1024 / 1024).toFixed(1)} MB</td><td>{batch.notificationStatus}</td><td><a className="table-action" href={`https://drive.google.com/drive/folders/${batch.driveFolderId}`} target="_blank" rel="noreferrer">Open Drive</a></td></tr>)}</tbody></table></div></section></AppShell>; }
