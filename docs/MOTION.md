# Motion

Motion has four jobs: establish depth, show cause and effect, preserve continuity, and confirm action. Controls use short press compression; important buttons add a pointer-origin ripple. Surfaces use small spring entrances and elevation-based hover rather than layout movement. Upload progress is driven by actual confirmed bytes, never a timer. `prefers-reduced-motion` removes spatial choreography and keeps essential state changes.
